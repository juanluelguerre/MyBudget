apiVersion: v1
kind: Service
metadata:
  name: {{ template "mybudgetweb.fullname" . }}
  labels:
    app: {{ template "mybudgetweb.name" . }}
    chart: {{ template "mybudgetweb.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "mybudgetweb.name" . }}
    release: {{ .Release.Name }}
